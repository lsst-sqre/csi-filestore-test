---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: csi-concurrent-scratch-pv1
  annotations:
    pv.kubernetes.io/provisioned-by: filestore.csi.storage.gke.io
spec:
  storageClassName: csi-filestore
  capacity:
    storage: 2.5Ti
  accessModes:
    - ReadWriteMany
  # Don't worry, this doesn't delete the underlying NFS data
  persistentVolumeReclaimPolicy: Delete
  volumeMode: Filesystem
  csi:
    driver: filestore.csi.storage.gke.io
    # Modify this to use the zone, filestore instance and share name.
    volumeHandle: "modeInstance/us-central1-b/csi-scratch/csi_scratch"
    volumeAttributes:
      ip: 10.161.1.18
      volume: csi_scratch
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: csi-concurrent-scratch-pv2
  annotations:
    pv.kubernetes.io/provisioned-by: filestore.csi.storage.gke.io
spec:
  storageClassName: csi-filestore
  capacity:
    storage: 2.5Ti
  accessModes:
    - ReadWriteMany
  # Don't worry, this doesn't delete the underlying NFS data
  persistentVolumeReclaimPolicy: Delete
  volumeMode: Filesystem
  csi:
    driver: filestore.csi.storage.gke.io
    # Modify this to use the zone, filestore instance and share name.
    volumeHandle: "modeInstance/us-central1-b/csi-scratch/csi_scratch"
    volumeAttributes:
      ip: 10.161.1.18
      volume: csi_scratch

